apiVersion: v1
kind: Service
metadata:
  name: service-front
  labels:
    app: service-front
    service: service-front
spec:
  type: LoadBalancer
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: service-front